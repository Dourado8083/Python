'''
 Enunciado: Escreva um programa que leia a quantidade de dias,
    horas, minutos e segundos do usuário. Calcule o total em segundos.
'''

# Entrada
dias = int (input ("Entre com a qtd de dias: "))
horas = int (input ("Entre com a qtd de horas: "))
minutos = int (input ("Entre com a qtd de minutos: "))
segundos = int (input ("Entre com a qtd de segundos: "))

# Processamento
segTotal = segundos + (minutos*60) + (horas*60*60) + (dias*24*60*60)

# Saida
print (f"{dias} dias + {horas}h{minutos}:{segundos} = {segTotal} segundos")
